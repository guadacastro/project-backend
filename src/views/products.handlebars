<h1>Productos</h1>
<ul>
  {{#each payload}}
  <li>
    <h2>{{this.title}}</h2>
    <p>{{this.description}}</p>
    <p>Precio: ${{this.price}}</p>
    <button onclick="location.href='/carts/addProduct/{{this._id}}'">Agregar al carrito</button>
  </li>
  {{/each}}
</ul>